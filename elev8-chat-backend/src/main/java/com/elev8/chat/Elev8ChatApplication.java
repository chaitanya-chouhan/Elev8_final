package com.elev8.chat;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Elev8 Community Chat backend.
 * Run this class to start the Spring Boot server on port 8080.
 */
@SpringBootApplication
public class Elev8ChatApplication {

    public static void main(String[] args) {
        SpringApplication.run(Elev8ChatApplication.class, args);
    }
}
