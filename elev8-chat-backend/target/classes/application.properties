# =============================================
# Elev8 Chat Backend - Application Configuration
# =============================================

# Server port (React runs on 5173, backend on 8080)
server.port=8080

# ---- H2 In-Memory Database ----
# "file:./data/elev8chat" is the DB name (saves to a file in the data folder)
spring.datasource.url=jdbc:h2:file:./data/elev8chat;AUTO_SERVER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Web Console (open http://localhost:8080/h2-console in browser to inspect DB)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ---- JPA / Hibernate ----
# update = create tables on startup if they don't exist, update schema without dropping
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
